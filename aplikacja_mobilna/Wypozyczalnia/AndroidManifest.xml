<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wypozyczalnia"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        >

        <activity
            android:name=".MainActivity"
            android:label="Wypożyczalnia" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Login"
            android:label="Zaloguj użytkownika" >
        </activity>
        
        <!-- All Product Activity -->
        <activity
            android:name=".AllCarsActivity"
            android:label="Wszystkie samochody" >
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name=".Register"
            android:label="Dodawanie użytkownika" >
        </activity>
        
        <!-- Edit Product Activity -->
        <activity
            android:name=".ShowCarActivity"
            android:label="Detale samochodu" >
        </activity>
        <!-- Edit Product Activity -->
        <activity
            android:name=".RentCarActivity"
            android:label="Wypożyczenie samochodu" >
        </activity>
        <activity
            android:name=".ChangePasswordActivity"
            android:label="Zmiana hasła" >
        </activity>
        <activity
            android:name=".ShowRentActivity"
            android:label="Detale wypożyczenia" >
        </activity>
        <activity
            android:name=".ChooseActivity"
            android:label="Wybierz działanie" >
        </activity>
    </application>

    <!--  Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>